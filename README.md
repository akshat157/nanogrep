# nanogrep - An extremely minimal command line utility to search for a query string in the given file.

This is a small utility program created as part of my practice of the [Rust book](https://doc.rust-lang.org/book/). Specifically, chapter 12, in which we build an extremely minimal version of the `grep` command line utility.
